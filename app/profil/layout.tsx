import React from "react";

export default function ProfilLayout({ children }: { children: React.ReactNode }) {
    return (
        <nav>
            <p> asdaasdas</p>
            {children}
        </nav>
    )
}